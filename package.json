{
  "name": "myasync",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -p tinyify index.js -o output.js",
    "watch": "npx watchify -v -p tinyify index.js -o output.js",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async-mqtt": "^2.6.1",
    "browserify": "^17.0.0",
    "mqtt": "^4.2.6",
    "tinyify": "^3.0.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "serve": "^11.0.2"
  }
}
